# Vendored upstream artifacts. Keep the bytes exactly as the .deb shipped them,
# so that re-running fetch-gir.py on a fresh clone produces no diff and the
# sha256 provenance in README.md stays meaningful. Without this, autocrlf would
# rewrite them to CRLF on checkout.
*.gir -text
